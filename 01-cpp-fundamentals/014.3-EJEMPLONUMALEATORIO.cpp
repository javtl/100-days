#include <iostream>
#include <stdlib.h>
#include <time.h>

using namespace std;

int main(){
        f
    int numero, dato , contador=0;
    sramd(time(NULL));
    dato = 1+rand()%(100); 

    do {cout<<"ingrese un numero: ";
            cin>>numero;
            if(numro=dato){
                cout<<"\ndigite un numero menor: ";

            }
            if(numero<dato){

                cout<<"digite un numero mayor";
            }
            contador++;
    } while(numero!= dato)

    cout<<"felicidades adivinaste el numero";
system(pause);
    return 0;
}